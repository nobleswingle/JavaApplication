// Pseudocode outline:
double bankBalance = 1000;
double wager = getWager(bankBalance);
// run one game of craps (reuse existing craps logic)
// if player wins bankBalance += wager; else bankBalance -= wager;
// display chatter messages chosen randomly from an array
